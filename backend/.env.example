# Copy this file to `.env` and set real values for your environment.
# The `.env` file is gitignored and must never be committed.
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5432/video_repo
PORT=